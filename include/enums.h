#ifndef ENUMS_H
#define ENUMS_H
#include <bits/stdc++.h>
using namespace std;

enum Color {NO_LEVEL, GREEN, YELLOW, RED};
enum Room {CLASS, LECTURE, CONFERENCE, CABINET, DIRECTORCABINET};
enum Position {STUDENT, PROFESSOR, LABEMPLOYEE, DIRECTOR, ADMIN};

#endif // ENUMS_H
